info:
  name: login
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_path}}/users/login"
  body:
    type: json
    data: |-
      {
        "email": "eduardo@email.com",
        "password": "123456"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
