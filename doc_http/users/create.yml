info:
  name: create
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_path}}/users"
  body:
    type: json
    data: |-
      {
        "name": "Eduardo",
        "email": "eduardo@email.com",
        "password": "123456"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
