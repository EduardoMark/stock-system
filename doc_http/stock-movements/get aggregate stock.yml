info:
  name: get aggregate stock
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_path}}/stock-movements/aggregate/:productId"
  params:
    - name: productId
      value: 5a200db8-9a77-4dd3-8644-1bbb197a4656
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
